import { SectionTitle, SectionWrapper } from "../sections.styles"

export const About = () => {
    return(
        <SectionWrapper>
            <SectionTitle>About Me</SectionTitle>
            <div>test1</div>
        </SectionWrapper>
    )
}