import { SectionTitle, SectionWrapper } from "../sections.styles"

export const Explore = () => {
    return(
        <SectionWrapper>
            <SectionTitle>Explore</SectionTitle>
            <div>test1</div>
        </SectionWrapper>
    )
}