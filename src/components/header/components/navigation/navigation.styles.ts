import styled from 'styled-components';

export const NavigationWrapper = styled.div`
    height: 75%;
    display: flex;
    justify-content: flex-end;
    margin-right: max(0px, calc((100vw - 1832px)/2));
    align-items: center;
`