import styled from 'styled-components';

export const AppWrapper = styled.div`
    height: 89vh;
    overflow-y: scroll;
    margin-top: 11vh;
    display: flex;
    flex-direction: column;
`;
